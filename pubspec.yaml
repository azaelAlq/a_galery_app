name: a_galery_app
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ^3.12.1

dependencies:
  firebase_auth: ^6.5.3
  firebase_core: ^4.11.0
  flutter:
    sdk: flutter
  google_sign_in: ^7.2.0
  provider: ^6.1.5+1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
